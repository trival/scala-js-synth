{
  "name": "web-synth-scala",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:scala": "scala-cli --power package --js . -f -o src/build",
    "watch:scala": "node watchScalaFiles.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "chokidar": "^4.0.0",
    "prettier": "^3.3.3",
    "terser": "^5.31.6",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@elemaudio/core": "^3.2.1",
    "@elemaudio/web-renderer": "^3.2.3"
  }
}
